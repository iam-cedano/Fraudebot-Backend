type: object
description: Paginated list of public contacts.
required:
  - data
  - total
  - page
  - count
properties:
  data:
    type: array
    description: Contacts on the current page.
    items:
      $ref: ./Contact.yaml
  total:
    type: integer
    minimum: 0
    description: Total number of matching contacts.
    example: 15
  page:
    type: integer
    minimum: 1
    description: Current page number (`p` query parameter).
    example: 1
  count:
    type: integer
    minimum: 1
    description: Page size (`c` query parameter).
    example: 10
